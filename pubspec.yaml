name: visualize
description: A new Flutter project.

publish_to: "none"

version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_riverpod: ^0.14.0+3
  google_fonts: ^2.1.0
  shared_preferences: ^2.0.5
  url_launcher: ^6.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.11.0

flutter:
  uses-material-design: true

  assets: 
    - assets/thumbnails/bubble.gif
    - assets/thumbnails/insertion.gif
    - assets/thumbnails/merge.gif
    - assets/thumbnails/quick.gif
    - assets/thumbnails/selection.gif
    - assets/thumbnails/heap.gif

